import type {FirebaseOptions} from 'firebase/app';

/**
 * Firebase web app options — skeleton only.
 * Do not commit real keys; load values from env / native config when you wire this up.
 *
 * TODO: connect to react-native-config, .env (babel), or similar — not implemented.
 */
export const firebaseConfig: FirebaseOptions = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
