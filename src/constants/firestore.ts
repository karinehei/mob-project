/**
 * Firestore collection ids — keep in sync with Console rules.
 */
export const FIRESTORE_COLLECTIONS = {
  sessions: 'sessions',
  evaluations: 'evaluations',
  samples: 'samples',
  questionnaires: 'questionnaires',
} as const;
