/**
 * App-level configuration placeholders (API base URL, feature flags, etc.).
 */
export const config = {
  appName: 'Food_Study',
  // TODO: read from env or remote config when needed
  apiBaseUrl: '',
} as const;
